```
llms-from-scratch/
├── README.md
├── requirements.txt
├── pyproject.toml            # 可选：现代 Python 项目配置
│
├── configs/                  # 实验配置（保证可复现）
│   ├── model/                # 模型结构相关配置
│   │   ├── gpt_small.yaml
│   │   └── gpt_medium.yaml
│   ├── data/                 # 数据相关配置
│   │   └── tiny_shakespeare.yaml
│   └── train.yaml            # 训练参数配置
│
├── data/                     # 数据文件（不放代码）
│   ├── raw/                  # 原始文本数据
│   ├── processed/            # 分词 / 预处理后的数据
│   └── README.md
│
├── src/
│   └── llms_from_scratch/    # ⭐ 核心源码包
│       ├── __init__.py
│
│       ├── tokenizer/        # 分词器（对应 Tokenization 章节）
│       │   ├── __init__.py
│       │   ├── basic.py      # 最简单的字符 / 单词分词器
│       │   └── bpe.py        # BPE 分词器
│       │
│       ├── data/             # 数据集 & 数据采样流水线
│       │   ├── __init__.py
│       │   ├── dataset.py    # Dataset 定义
│       │   └── sampler.py    # 采样策略（随机 / 分层等）
│       │
│       ├── model/            # GPT 模型实现
│       │   ├── __init__.py
│       │   ├── embeddings.py # Token / Position Embedding
│       │   ├── attention.py  # 自注意力（Causal Self-Attention）
│       │   ├── transformer.py# Transformer Block
│       │   └── gpt.py        # GPT 主模型
│       │
│       ├── loss/             # 损失函数 & 指标
│       │   ├── __init__.py
│       │   └── cross_entropy.py
│       │
│       ├── optim/            # 优化器 & 学习率调度
│       │   ├── __init__.py
│       │   └── adamw.py
│       │
│       ├── train/            # 训练逻辑
│       │   ├── __init__.py
│       │   ├── trainer.py    # Trainer 封装
│       │   └── loop.py       # 训练 / 验证循环
│       │
│       ├── eval/             # 评估 & 文本生成
│       │   ├── __init__.py
│       │   └── generate.py
│       │
│       └── utils/            # 工具函数
│           ├── __init__.py
│           ├── seed.py       # 随机种子控制
│           └── logger.py     # 日志封装
│
└── scripts/                  # 命令行入口
    ├── train.py              # 启动训练
    ├── eval.py               # 模型评估
    └── sample.py             # 文本生成示例
```